---
- hosts: 'master'
  become: yes
  become_user: kube

  tasks:
    - name: Create PVC to Store Linux ISOs
      kubernetes.core.k8s:
        definition:
          apiVersion: v1
          kind: PersistentVolumeClaim
          metadata:
            name: storage
            namespace: usenet
            labels:
              app: usenet
          spec:
            storageClassName: nfs-client
            accessModes:
              - ReadWriteMany
            resources:
              requests:
                storage: 100Gi
